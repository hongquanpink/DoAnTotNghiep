@model List<Model_Data.Framework.ProductCategory>

<div class="category">
    <h3>Danh mục sản phẩm</h3>
    <ul id="sideManu" class="nav nav-tabs nav-stacked">
        @foreach (var item in Model)
        {
            <li >
            <a style=" text-decoration:auto;font-size:16px; color: #000000; display: block;" href=" /san-pham/@item.ID">@item.Name</a>
            </li>
        }
    </ul>
</div>
